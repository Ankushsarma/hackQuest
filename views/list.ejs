<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quest</title>
    <style>
        body {
            background-color: #0A0A23;
            color: white;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }
        .quest_container {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        .cule_form button {
            width: 100%;
            padding: 10px;
            border: 1px solid #1E90FF;
            border-radius: 5px;
            background-color: #1C1C2F;
            color: white;
            cursor: pointer;
            font-size: 16px;
        }
        .cule_form button:disabled {
            background-color: #555;
            cursor: not-allowed;
        }
        .finalAnswer {
            margin-top: 20px;
        }
        .finalAnswer button {
            padding: 10px;
            border: 1px solid #1E90FF;
            border-radius: 5px;
            background-color: #1C1C2F;
            color: white;
            cursor: pointer;
            font-size: 16px;
        }
        .finalAnswer button:disabled {
            background-color: #555;
            cursor: not-allowed;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar'); %>
    <h1>Welcome, <%= username %>!</h1>
    <div class="quest_container">
        <% for (let i = 0; i < 4; i++) { %>
            <form class="cule_form" action="/quest" method="post">
                <input type="hidden" name="quest_index" value="<%= i %>">
                <button type="submit" <%= completedQuests[i] ? 'disabled' : '' %>>Quest <%= i + 1 %></button>
            </form>
        <% } %>
    </div>
    <div class="finalAnswer">
        <a href="<%= !finalAnswerAttempted ? '/finalAnswer' : '#' %>">
            <button <%= !finalAnswerAttempted ? '' : 'disabled' %>>Submit Final Answer</button>
        </a>
    </div>
</body>
</html>